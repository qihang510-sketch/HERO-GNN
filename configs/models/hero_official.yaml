name: hero_official
display_name: HERO-official
implementation_source: project
forbidden_lite_name: false
official_hetero_score: feature_relation_degree
risk_relevance_threshold: 0.3
hetero_branch_weight: 0.6
fusion_type: concat_linear
class_weight_strategy: inverse_frequency
use_class_weight: true
learning_rate: 0.001
dropout: 0.2
weight_decay: 0.0001
loss_type: bce
focal_gamma: 2.0
optimizer: adamw
scheduler: reduce_on_plateau
scheduler_patience: 10
scheduler_factor: 0.5
scheduler_min_lr: 0.000001
early_stopping_patience: 30
early_stopping_metric: AUPRC
risk_weight_temperature: 1.0
