suite: main
datasets:
  - yelp_academic
  - amazon_video
  - fraud_yelp
  - fraud_amazon
  - elliptic
models: null
seeds: [0, 1, 2, 3, 4]
device: cuda
output_dir: outputs/submission_unified
metrics:
  - Macro-F1
  - AUROC
  - AUPRC
notes:
  - null models means use the dataset-specific submission matrix in src.training.submission.
  - missing raw results must remain missing or skipped, never imputed.
