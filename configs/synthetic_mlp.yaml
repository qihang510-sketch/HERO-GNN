experiment:
  name: synthetic_mlp
  seed: 42
dataset:
  name: synthetic
  processed_dir: data/processed/synthetic
  num_nodes: 200
  num_edges: 800
  feature_dim: 16
model:
  name: mlp
  hidden_dim: 64
training:
  epochs: 10
  lr: 0.001
outputs:
  root: outputs

