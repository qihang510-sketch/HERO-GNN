experiment:
  name: amazon_video_hero
  seed: 42
dataset:
  name: amazon_video
  raw_dir: data/raw/amazon_video
  processed_dir: data/processed/amazon_video
  label_type: proxy
model:
  name: hero_gnn
  hidden_dim: 64
  use_heterophily: true
  use_mechanism: true
  use_chain: true
training:
  epochs: 10
  lr: 0.001
outputs:
  root: outputs

